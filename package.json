{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^18.11.18",
    "lerna": "^6.3.0",
    "typescript": "^4.9.4"
  },
  "scripts": {
    "publish": "git commit -am \"pub\" && git push --no-verify && lerna run tsc && lerna publish"
  }
}
